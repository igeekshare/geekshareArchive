import AdminPlaceholder from "../_components/AdminPlaceholder";

export default function SeoPage() {
  return (
    <AdminPlaceholder
      title="SEO 设置"
      description="站点搜索引擎元数据配置将在后续阶段接入，本阶段仅提供页面入口。"
    />
  );
}
