import AdminPlaceholder from "../_components/AdminPlaceholder";

export default function SitePage() {
  return (
    <AdminPlaceholder
      title="站点设置"
      description="站点级配置功能将在后续阶段接入，本阶段仅提供页面入口。"
    />
  );
}
