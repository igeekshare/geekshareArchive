import AdminPlaceholder from "../_components/AdminPlaceholder";

export default function SyncPage() {
  return (
    <AdminPlaceholder
      title="同步管理"
      description="频道同步任务与日志功能将在后续阶段接入，本阶段仅提供页面入口。"
    />
  );
}
